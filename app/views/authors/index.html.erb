<h1>All Authors</h1>

<ul>
  <% @authors.each do |a| %>
   <li><%= link_to(a.name, author_path(a.id)) %> has <%= pluralize(a.articles.length, 'article') %></li>
  <% end %>
</ul>

<p><%= link_to( 'Add New Author', new_author_path, :class => 'btn btn-default' ) %></p>